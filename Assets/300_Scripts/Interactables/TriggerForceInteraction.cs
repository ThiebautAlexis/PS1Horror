using EnhancedEditor;
using System;
using UnityEngine;

namespace HorrorPS1
{
    /*
    public class TriggerForceInteraction : MonoBehaviour
    {
        #region Fields and Properties

        #endregion

        #region Methods 
        private void OnTriggerEnter(Collider other)
        {
            if(TryGetComponent(out IInteractable _forcedInteraction))
            {
                _forcedInteraction.Interact();
            }
        }
        #endregion
    }
    */
}
